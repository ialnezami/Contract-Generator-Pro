import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import '../styles/globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Contract Generator Pro',
  description: 'A powerful, user-friendly contract generation platform built for freelancers, small businesses, and legal professionals.',
  keywords: ['contracts', 'legal documents', 'freelance', 'business', 'templates'],
  authors: [{ name: 'Contract Generator Pro Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-background">
          {children}
        </div>
      </body>
    </html>
  )
}
